import NotFoundPage from "@/components/ui/NotFoundPage";

export const metadata = {
  title: "Page Not Found | Everence",
  robots: { index: false, follow: false },
};

export default function NotFound() {
  return <NotFoundPage />;
}
